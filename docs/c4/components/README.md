# L3 component diagrams — generated lazily

One `.mmd` per container, created on demand by
`.claude/skills/generate-component-l3` when that container is about to be
built or significantly changed. Speculative L3 for the whole system is
deliberately prohibited: it would be wrong by the time it is read, and it
bloats context. If the diagram you need is missing, run the skill.
